/// This class is used in the [subtitle1_item_widget] screen.
class Subtitle1ItemModel {Subtitle1ItemModel({this.subtitle, this.month, this.copied, this.id, }) { subtitle = subtitle  ?? "paid on";month = month  ?? "October 22nd 2023";copied = copied  ?? "copied";id = id  ?? ""; }

String? subtitle;

String? month;

String? copied;

String? id;

 }
