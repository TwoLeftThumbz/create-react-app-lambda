/// This class is used in the [cancelled_item_widget] screen.
class CancelledItemModel {CancelledItemModel({this.price, this.email, this.lastDays, this.id, }) { price = price  ?? "€721";email = email  ?? "morticia.adams@email.com";lastDays = lastDays  ?? "Pending";id = id  ?? ""; }

String? price;

String? email;

String? lastDays;

String? id;

 }
